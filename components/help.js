export default function HelpMessage() {
  return (
    <div id="help-message">
      <p>                                                                                              Click the <span class="material-icons">bolt</span> in the sidebar, or press <code>Ctrl + a</code> to create a new ad hoc meeting.
      </p>
      <p>Click a tick on the timeline to create a note for that time.
        Click anywhere in the timeline box to create a note for "now".
      </p>
      <p>
        Need help? Press <code>Ctrl + /</code>
      </p>
    </div>
  );
}
